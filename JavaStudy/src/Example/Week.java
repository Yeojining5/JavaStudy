package Example;

public enum Week { // Week라는 열거 타입 선언 
	MONDAY,
	TUESDAY,
	WEDNESDAY,
	THURSDAY,
	FRIDAY,
	SATURDALY,
	SUNDAY
	
}

/*   Week todya;
 *   Week resevationDay;  이런식으로 변수를 선언
 *   Week today = Week.SUUNDAY;  >  today 열거 변수에 열거 상수인 Sunday를 저장
 *   Week birthday = null;   >  null값도 저장 가능 (참조 타입이기 때문)
 *   열거 상수는 열거 객체로 생성되므로 객체이다
 *   !!참조 타입 변수는 객체를 참조하는 변수!!
 *   
 */
	
	